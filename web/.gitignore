# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# Vite: `.env.*.local` など（ルート `.gitignore` の `.env.*.local` と併用）
*.local
# `cp .env.example .env` した場合のみ除外（`.env.development` 等はコミット対象）
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
