[project]
name = "glowbe-esp32-pio"
version = "0.1.0"
description = "Pinned PlatformIO CLI for Glowbe ESP32 firmware (uv-managed .venv)."
requires-python = ">=3.11"
dependencies = [
    "intelhex>=2.3.0",
    "platformio>=6.1.18,<7",
]
