# Copy to glowbe.firmware.env (local, not committed).
# Loaded automatically on `pio run` / `pio upload`.
# Variables already set in the shell take precedence.

# All-white current cap (mA). Default when unset: 3200.
GLOWBE_MAX_CURRENT_MA=3200
